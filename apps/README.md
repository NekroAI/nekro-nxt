# Apps

- `server`：生产 DSH Host roster、真实 Agent Loop、Episode handoff 与 Channel Runtime 适配层；
- `web`：NekroNxt Shell 与 DSH Client/Slot 兼容基础，后续演进为共享 Web UI。

Desktop 在 M7 进入；Server 与 Web 复用同一 Core 契约，不创建第二套领域实现。
