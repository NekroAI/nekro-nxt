{
  "name": "@nekro-nxt/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.mjs",
  "scripts": {
    "build": "node scripts/clean-server-runtime.mjs && tsdown && node scripts/write-product-release.mjs",
    "dist": "node scripts/dist.mjs",
    "dist:dir": "node scripts/dist.mjs --dir",
    "test": "vitest run --root ../.. apps/desktop/tests",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@electron/rebuild": "4.2.0",
    "electron": "42.0.1",
    "electron-builder": "26.8.1"
  }
}
