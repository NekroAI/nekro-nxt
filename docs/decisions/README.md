# NekroNxt 决策记录

这里只记录公共协议、领域模型、持久格式、DSH 集成、Extension/Adapter Runtime 和重要产品行为。

- `proposed/`：尚未实施、仍可能调整；
- `implemented/`：已成为当前设计或实现事实；
- `rejected/`：认真考虑后明确拒绝，保留原因避免重复争论。

模板与写作规则见 `../05-知识体系与AI开发流程.md`。普通功能、局部修复和临时计划不需要 Decision Note。
