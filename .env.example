# Optional deployment overrides. Model providers and API keys are configured in the Web settings page.
NEKRO_PORT=4960
NEKRO_DATA=data
# Optional advanced Host override; each intelligent-agent still uses its own <agentId> child.
# NEKRO_DEVELOPMENT_WORKSPACE_ROOT=/absolute/path/to/workspaces
