# NekroNxt 开发与知识约束

本文件是开发者、AI 编码工具和自动化任务的统一入口。规则使用中文沉淀；项目早期只保留必要约束，不提前引入复杂门禁。

## 1. 产品身份

- 产品名：`NekroNxt`；
- 根包名与仓库名：`nekro-nxt`；
- 定位：以 DSH 为核心引擎、原生群聊、兼具娱乐性和生产力、可由智能体参与扩展开发的高扩展聊天系统；
- Nekro Agent 仅作为历史经验来源，不做数据迁移、插件兼容或旧架构复刻。

## 2. 术语铁律

- 面向用户的唯一实体称呼是“智能体”；
- UI、帮助文档、通知和错误信息不得用 Agent、AI 成员、Bot、机器人或助手指代智能体；
- “助手”只可作为具体智能体的名称或人设角色；
- 外部聊天平台的发送账号可以称“机器人账号”，但不得与智能体混为一个对象；
- 内部 TypeScript 类型允许使用 `AgentDefinition`、`AgentRevision`、`AgentSession`、`AgentActivation`，不得直接泄漏到用户界面；
- DSH 原生概念如 Agent Loop 可在开发文档中使用，首次出现时必须解释其产品含义。

详细映射见 `docs/01-术语与文案规范.md`。

## 3. 开发方式

- 纯 TypeScript 主栈，ESM 优先；
- 使用 pnpm workspace 的单一仓库；
- Desktop、Server、Web UI、社区后端和共享包最终都位于本仓库；
- 先形成少量清晰包，禁止为了“未来可能复用”过早拆出大量包；
- 根命令应逐步收敛为 `pnpm dev`、`pnpm check`、`pnpm test`、`pnpm build`；
- 重要架构决策和踩坑结论使用中文写入 `docs/`，不要只留在聊天记录中。
- 抽象、包和配置项必须有当前所有者与消费者；无法说明删除它会破坏什么行为或测试时，不提前引入；
- 状态只在真实提交点发布，注册必须可撤销，dispose 必须等待资源静止；详细规范见 `docs/06-开发与测试规范.md`。

## 4. DSH 与扩展体系

- DSH 是外部核心智能体引擎，优先通过公开 API、插件、Bundle、Preset 和组合层扩展；
- 不维护深度 Fork，不复制 DSH 内核；必须修改上游行为时，先记录兼容层边界和升级成本；
- NekroNxt 自身尽量以 Cordis 插件/Bundle 组合接入，不把自己设计成不可替换的特权模块；
- Adapter、Tool、Preset、MCP 接入和 UI Slot 都是统一 Extension 的不同 Contribution；
- “添加连接”必须先消费已安装 `AdapterContribution` 的平台目录，再按其版本化配置 schema 创建 Connection；不得让 QQ 或其他单个平台占用通用入口，系统托管 Adapter 不出现在用户创建列表；
- “NekroNxt 扩展”只是一种元数据标记，表示支持创造工作台、热重载和产品集成，不是第二套插件系统；
- 首版不建设社区市场、签名、SBOM 和复杂供应链，只保留未来所需的数据边界。

## 5. 产品与运行时边界

- 用户显式创建智能体，再为其设置人设、模型、能力、频道和扩展；
- 一个频道只包含自己的消息事实流，不把多个频道的短期上下文直接混合；
- 工具执行期间的新消息可靠入库，在安全间隙注入下一步思考；普通消息不得隐式取消当前工具；
- 所有用户可见消息必须由智能体调用通信工具产生，包括 Web Channel 的文字回复；
- DSH 模型原始 `text/reasoning` 只作为内部运行轨迹和后台观察，不自动投递到频道；
- Mention、图片、文件等内容统一使用通信工具的结构化内容块，不通过提示词标记或宿主路径传递；
- 智能体配置和扩展均通过不可变版本演进；运行中的会话不在工具执行中热替换；
- 动态运行、保存扩展版本、给智能体启用扩展是三个独立动作；
- 产品界面说明操作对象、作用范围和结果，不用含糊的“确定”“能力”“查看”替代真实动作。
- 用户可见操作控件不得是空处理器或无结果占位；未实现能力必须永久禁用并明确标注“尚未开放”，不能呈现为可点击按钮。
- 未实现能力不得长期占据一级导航、页面主操作或大面积正文；只有用户必须了解前置条件时才保留产品化说明，开发阶段和内部实现进度进入文档与诊断日志。

## 6. 动态创造

- 首版优先打通本地闭环：描述需求 → 生成动态包 → 运行 → 验证 → 修改 → 保存本地扩展 → 给指定智能体启用；
- 动态创造本身不隐式获得开发 Shell 或完整文件访问；三种能力分别由用户配置；
- 验证展示真实构建、调用、渲染和停止结果，不使用“智能体声称成功”作为唯一证据；
- 不在项目尚未验证前引入重型审批、安全分级和社区审核流程。

## 7. 交付形态

- Desktop：Windows/macOS 安装即用，不要求用户预装 Node、pnpm、Python 或 Docker；
- Server：面向 7×24 小时运行，目标是单容器、一个主要 `/data`、少量必要配置；
- 两种宿主复用核心、数据模型、Web UI 和扩展体系；
- 升级必须可理解、可验证、失败可恢复，不在运行容器中执行 `git pull`。

## 8. 修改完成条件

- 与任务相关的类型检查、测试或手工验证已经执行；
- 测试验证真实入口和外部结果，不把智能体或被测代码自称成功当作证据；
- 用户可见 Web 行为发生变化时，必须运行生产构建的产品旅程测试；SSR 静态标记、API 测试和构建成功都不能单独证明页面可用；
- 产品旅程必须把 `pageerror`、React `console.error`、未处理拒绝、空白根节点和关键页面不可见视为硬失败；浏览器控制不可用不能降级为“未验证但已完成”；
- UI/UX 改动必须同时完成源码审查与真实截图视觉审查；空间分配、边距、对齐、层叠、滚动、主题、视口、空态、错误态、disabled/loading 和文案任一明显不合格时不得以功能可用或 Console 无错替代；
- 用户可见文案符合“智能体”术语规则；
- 新行为没有破坏频道隔离、版本边界和扩展生命周期；
- 对产品认知有影响的变更同步更新中文文档；
- 不在子目录创建嵌套 Git 仓库。

## 9. 按任务类型查阅

根文件只保存常驻规则，详细知识按任务读取：

- 任何任务：读取相关 package README、当前类型、测试和最近的局部 `AGENTS.md`；
- 前端 UI、用户可见文案、布局、样式、主题、弹层或交互：必须读取 `docs/01-术语与文案规范.md`、`docs/05-桌面UI与动效规范.md`、`docs/06-开发与测试规范.md` 和 `docs/NekroNxt界面交互模型.md`；
- 新功能、领域模型、持久格式、公共 API、DSH、Extension、Adapter、消息、Runtime、部署或社区设计：必须核对 `docs/02-未来扩展方向.md`；
- 消息、Channel、Adapter 或模型输出：必须读取 `docs/03-消息内容与投递协议.md` 和对应 implemented Decision Note；
- 生命周期、并发、持久化、migration、测试策略或跨包改动：必须读取 `docs/06-开发与测试规范.md` 和对应 accepted/implemented Decision Note；
- 新架构或新能力：必须读取 `docs/07-参考项目复用指南.md`，先搜索本仓现有设计、类型和测试；涉及 DSH 时只依赖其公开 API、发布包、公开源码和可复现测试；
- 文档公开边界遵循 `docs/00-文档公开边界.md`；本地 `.local/` 资料不得被公开文档链接或引用；
- 普通局部修复不必通读所有未来规划，但不得改变公共契约和产品铁律。

涉及未来方向和参考项目的任务，完成前说明：关联了哪些未来方向、保留了什么接缝、是否锁死未来、本次明确未提前实现什么，以及参考了哪个项目、借鉴与拒绝了什么。
