# Desktop 多实例与设备鉴权

状态：implemented

## 问题

Server 适合在 VPS、NAS 或家庭服务器上持续运行，Desktop 适合提供本机窗口、通知和操作系统适配。单一 BrowserWindow 固定加载本地 Host 时，用户无法用一个客户端管理多台独立服务器；直接把管理密钥交给远程产品页面，又会让 Extension Client 与普通页面脚本进入本机可信边界。

## 决定

Desktop 是本地实例与多个远程服务实例的切换宿主。每个实例拥有独立的数据、智能体、频道、扩展、凭据和运行状态；切换只替换当前管理界面，不停止任何实例中的 Runtime。

远程实例加载该 Server 同包托管的 Web UI。Desktop 只协商 `managementProtocol = 1` 与 `desktopChromeProtocol = 1`，不维护跨任意历史业务 API 的兼容层。实例版本不兼容时进入可信错误页，不加载产品页面。

BrowserWindow 的 Content View 下维护：

- 当前实例的 `Product WebContentsView`，使用 Profile 专属持久 partition；
- 安装包内的 `Instance Overlay WebContentsView`，负责实例列表、配对、编辑、重认证和移除；
- 本地 `Trusted Fallback WebContentsView`，负责连接、认证与不兼容状态。

切换实例时关闭旧 Product View，再为目标 partition 创建新 View。旧 View 的 Zustand、SSE、动态 Client、Extension Client 和请求随 WebContents 一起销毁。每个 Profile 保存自己的最近产品路由。页面通过统一未保存草稿注册器声明编辑状态；跨实例切换前由主进程显示确认。

## Desktop 信任边界

远程 Product View 使用 `contextIsolation: true`、`nodeIntegration: false`、`sandbox: true`。它只获得：当前实例展示信息、订阅当前状态和请求打开实例浮层。Bridge 不返回 Profile 列表、地址、设备凭据或固定公钥，不提供文件、Shell、通知和通用 IPC。

实例浮层使用独立本地 preload。Profile 与设备 Secret 分离：Profile 使用版本信封和原子替换文件；损坏或未来版本原文件改名保留为 recovery 文件。设备 Secret 通过 Electron `safeStorage` 加密；Linux 选择 `basic_text` 或安全存储不可用时只保留进程内凭据，应用重启后要求重新认证。

每个远程 partition 固定服务器 SPKI SHA-256。首次配对由主进程临时读取实际 TLS 证书并完成 HMAC 证明；后续 Chromium 证书回调只接受相同 host 与固定 SPKI。身份或公钥变化进入重新认证，不自动覆盖。

Desktop 为启用通知的远程 Profile 进行限频状态检查，生成服务离线、恢复、设备会话失效和动态创造等待确认的系统通知。点击通知只打开白名单内产品相对路由。Product View 的 Web Notification 权限永久拒绝，通知由主进程生成。

## Server 安全入口

开发与 Desktop 自带本地 Host 继续使用随机 loopback HTTP。`NEKRO_HOST=0.0.0.0` 时必须提供至少 32 个字符的 `NEKRO_MANAGEMENT_KEY`；缺失、空白或过短均拒绝启动。管理密钥只保留在进程与配对 HMAC 计算中，数据库只保存带域分隔的摘要，用于识别密钥轮换。轮换会撤销全部旧设备。

安全入口在同一进程中监听外部 TLS，并把已认证请求代理到随机 loopback DSH WebServer。首次启动在 `/data/host/tls/` 生成 EC P-256 自签名证书和私钥；容器重启复用同一身份。用户只配置服务器地址与管理密钥。

配对协议使用一次性 60 秒 challenge：Server nonce、Client nonce、实例 ID 与实际 SPKI 进入 `HMAC-SHA-256` proof。管理密钥不进入请求正文。验证成功后 Server 签发随机设备 Secret，只保存带域分隔的 Secret 摘要。Desktop 用设备 Secret 换取 12 小时 Secure、HttpOnly、SameSite=Strict Session Cookie 与独立 CSRF Token。

匿名入口只有：

- `GET|HEAD /health/live`；
- `GET|HEAD /health/ready`；
- `GET|HEAD /.well-known/nekro-nxt`；
- `POST /api/management/pairing/challenge`；
- `POST /api/management/devices/enroll`；
- `POST /api/management/session`。

其余静态资源、SPA、REST、SSE、Asset、Extension Client 模块与 RPC 默认要求有效设备 Session。非安全方法同时校验 Session、同源 `Origin`/`Host`、CSRF Cookie 与 `X-Nxt-Csrf`。安全入口不发布通用 CORS。

实例身份、管理密钥摘要和配对设备保存在 `core.sqlite`；证书属于 `/data/host/tls/`。移除 Desktop Profile 会清除对应 partition 与本机 Secret，并尽力撤销 Server 设备；Server 离线时本地清理仍可完成。远程实例的数据不属于 Desktop Profile 删除范围。

## 界面

Desktop 模式在左侧图标轨底部把“服务实例”放在主题按钮下方。按钮显示当前实例状态；普通浏览器不渲染该入口。按钮右侧打开 344px 宽、最大 480px 高的可信浮层：本地实例固定第一项，远程实例按添加顺序排列，标题和底部添加操作固定，列表独立滚动。

实例行显示名称、此设备或 `host:port`、明确状态与当前勾选。远程实例提供重新连接、修改名称或地址、系统通知开关、重新认证和移除。添加表单只要求显示名称、服务器地址和管理密钥；地址缺省端口为 4960，拒绝协议降级、账号密码、路径、query 与 fragment。

Desktop 在每次 Product View `dom-ready` 后注入当前平台窗口安全区：macOS 左侧 84px，Windows/Linux 右侧 138px。远程与本地页面使用同一 48px 顶栏和导航外壳，页面不按 user agent 推测窗口控件位置。

## 不在此决定内

- 跨实例数据同步或统一智能体/频道列表；
- 自动故障转移；
- 本地目录绑定、文件传输、远程挂载或通用系统能力 Bridge；
- 社区账号与中心控制面；
- Extension Client 直接调用系统通知。

## 验证

Server 测试覆盖匿名拒绝、HMAC 配对、challenge 重放、Session、CSRF、代理业务请求和设备撤销。Desktop 测试覆盖地址规范化、固定本地 Profile、远程添加顺序、移除边界和损坏 Profile 恢复。生产旅程继续证明普通浏览器不显示 Desktop 实例入口；Electron 旅程负责多 partition、完整 View 替换、平台安全区、可信浮层层级和通知回跳。
